/* eslint no-unused-vars: "off" */

// Values are set by PreSonus
// Channel values can be found in the `channels` enum
export enum CHANNELTYPES {
    LINE = 'line',
    MAIN = 'main',
    TALKBACK = 'talkback',
    AUX = 'aux',
    SUB = 'sub',
    FX = 'fxbus',
    FXRETURN = 'fxreturn',
    RETURN = 'return' // e.g. Aux In 1
}
